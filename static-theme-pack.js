'use strict';
const fs=require('fs/promises'),path=require('path'),crypto=require('crypto'),express=require('express'),topojson=require('topojson-client'),worldAtlas=require('world-atlas/countries-50m.json');const{renderStaticPng}=require('./static-render'),{decodePng,encodePng}=require('./png-codec'),{earthPng}=require('./earth-texture');
const SNAPSHOT=path.join(__dirname,'data','public-snapshot.json'),world=topojson.feature(worldAtlas,worldAtlas.objects.countries),EXTRA=new Set(['midnight','aurora','amber','mono','ice','earth']),cache=new Map(),W=640,H=500,GX=320,GY=170,GR=154,MX=16,MY=16,MW=608,MH=306;
function bool(v,f=true){if(v==null||v==='')return f;return!['0','false','off','no'].includes(String(v).toLowerCase())}function opts(q){return{projection:q.projection==='mercator'?'mercator':'globe',theme:String(q.theme||'').toLowerCase(),showName:bool(q.name),showStats:bool(q.stats),showLegend:bool(q.legend),showDxcc:bool(q.dxcc),showUpdated:bool(q.updated)}}const clamp=v=>Math.max(0,Math.min(255,Math.round(v)));
function tc(t,r,g,b){if(t==='midnight')return[clamp(r*.32),clamp(g*.5),clamp(b*.82+18)];if(t==='aurora')return[clamp(r*.48),clamp(g*.92+16),clamp(b*.8+24)];if(t==='amber')return[clamp(r*.92+28),clamp(g*.62+16),clamp(b*.27)];if(t==='mono'){const y=clamp(r*.299+g*.587+b*.114);return[y,y,y]}if(t==='ice')return[clamp(r*.64+34),clamp(g*.82+26),clamp(b*.98+28)];return[r,g,b]}
function transform(body,t){const img=decodePng(body,{maxPixels:W*H+10});for(let i=0;i<img.data.length;i+=4){const c=tc(t,img.data[i],img.data[i+1],img.data[i+2]);img.data[i]=c[0];img.data[i+1]=c[1];img.data[i+2]=c[2]}return encodePng(img.width,img.height,img.data)}
function sample(tex,lat,lon){lon=((lon+180)%360+360)%360-180;const x=Math.max(0,Math.min(tex.width-1,Math.floor((lon+180)/360*tex.width))),y=Math.max(0,Math.min(tex.height-1,Math.floor((90-lat)/180*tex.height))),i=(y*tex.width+x)*4;return[tex.data[i],tex.data[i+1],tex.data[i+2]]}
function inv(nx,ny,z2,clat,clon){const ry=-clon*Math.PI/180,cy=Math.cos(ry),sy=Math.sin(ry),rx=clat*Math.PI/180*.72,cx=Math.cos(rx),sx=Math.sin(rx),y=ny*cx+z2*sx,z1=-ny*sx+z2*cx,x=nx*cy-z1*sy,z=nx*sy+z1*cy;return{lat:Math.asin(Math.max(-1,Math.min(1,y)))*180/Math.PI,lon:Math.atan2(x,z)*180/Math.PI}}
function blendEarth(baseBody,texBody,projection,home){const base=decodePng(baseBody,{maxPixels:W*H+10}),tex=decodePng(texBody,{maxPixels:1_000_000});function blend(x,y,c,a=.62){const i=(y*W+x)*4;base.data[i]=clamp(base.data[i]*(1-a)+c[0]*a);base.data[i+1]=clamp(base.data[i+1]*(1-a)+c[1]*a);base.data[i+2]=clamp(base.data[i+2]*(1-a)+c[2]*a)}if(projection==='mercator'){for(let y=MY;y<MY+MH;y++){const v=(1-2*((y-MY)/MH))*Math.PI,lat=Math.atan(Math.sinh(v))*180/Math.PI;for(let x=MX;x<MX+MW;x++)blend(x,y,sample(tex,lat,(x-MX)/MW*360-180),.7)}}else{const clat=Number(home?.lat)||0,clon=Number(home?.lon)||0;for(let y=GY-GR;y<=GY+GR;y++)for(let x=GX-GR;x<=GX+GR;x++){const nx=(x-GX)/GR,ny=(GY-y)/GR,d=nx*nx+ny*ny;if(d>1)continue;const g=inv(nx,ny,Math.sqrt(1-d),clat,clon);blend(x,y,sample(tex,g.lat,g.lon),.68)}}return encodePng(W,H,base.data)}
async function image(o){const stat=await fs.stat(SNAPSHOT),key=[o.projection,o.theme,o.showName,o.showStats,o.showLegend,o.showDxcc,o.showUpdated].join(':'),old=cache.get(key);if(old&&old.mtimeMs===stat.mtimeMs&&old.size===stat.size)return old;const data=JSON.parse(await fs.readFile(SNAPSHOT,'utf8')),baseTheme=o.theme==='earth'?'clean':o.theme==='amber'?'rough':o.theme==='ice'?'clean':'futuristic';let body=renderStaticPng(data,world,{...o,theme:baseTheme});if(o.theme==='earth'){try{body=blendEarth(body,await earthPng(),o.projection,data.settings?.home)}catch{}}else body=transform(body,o.theme);const item={body,mtimeMs:stat.mtimeMs,size:stat.size,etag:`"${crypto.createHash('sha256').update(body).digest('base64url')}"`};cache.set(key,item);if(cache.size>64)cache.delete(cache.keys().next().value);return item}
const originalListen=express.application.listen;express.application.listen=function(...args){this.get('/static/qrz.png',async(req,res,next)=>{const o=opts(req.query||{});if(!EXTRA.has(o.theme))return next();try{const item=await image(o);res.set('ETag',item.etag);if(req.get('if-none-match')===item.etag)return res.status(304).end();return res.type('image/png').send(item.body)}catch(e){return next(e)}});return originalListen.apply(this,args)};
module.exports={EXTRA};
