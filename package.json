{
  "name": "qso-trails",
  "version": "0.2.0",
  "private": true,
  "description": "Self-hosted Wavelog/ADIF QSO path map with admin-controlled public iframe view",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "NODE_ENV=development node --watch server.js",
    "check": "node --check server.js && node --check public/admin.js && node --check public/globe.js",
    "audit:prod": "npm audit --omit=dev --audit-level=high"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "express": "5.2.1",
    "multer": "2.2.0",
    "topojson-client": "3.1.0",
    "world-atlas": "2.0.2"
  }
}
