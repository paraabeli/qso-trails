'use strict';
const assert=require('assert'),{encodePng,decodePng}=require('../png-codec');
const rgba=Buffer.from([255,0,0,255,0,255,0,255,0,0,255,255,255,255,255,128]);
let sourceCopies=0;const originalFrom=Buffer.from;Buffer.from=function(value,...args){if(value===rgba)sourceCopies++;return originalFrom.call(Buffer,value,...args)};let png;try{png=encodePng(2,2,rgba)}finally{Buffer.from=originalFrom}assert.strictEqual(sourceCopies,0,'encodePng must not copy the entire RGBA buffer per row');assert.strictEqual(png.subarray(0,8).toString('hex'),'89504e470d0a1a0a');const d=decodePng(png,{maxPixels:4});assert.strictEqual(d.width,2);assert.strictEqual(d.height,2);assert.deepStrictEqual(d.data,rgba);assert.throws(()=>decodePng(Buffer.from('not-png')),/PNG|signature/i);assert.throws(()=>decodePng(png,{maxPixels:3}),/dimensions|limit/i);console.log('PNG codec regression tests passed.');
